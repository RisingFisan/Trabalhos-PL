int cont = 0;
int num;

print("Indique os números naturais (insira 0 para parar):");
while(num != 0) {
    num = int(input(""));
    if num > 0 && num % 2 != 0{
        cont++;
        println(num);
    }
}

print("Total: ");
print(cont):
println(" números ímpares.");